<apex:page controller="owd" >
    <apex:outputText value="{!beforedate}" ></apex:outputText>
    <apex:pageBlock >
    <apex:pageBlockTable value="{!wList}" var="w" >
        <apex:column value="{!w.Id}" />
        <apex:column value="{!w.update__c}" />
        <apex:column value="{!w.onlydate__c}" />
        </apex:pageBlockTable>
    </apex:pageBlock>
</apex:page>